index.html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>For You 💗</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(180deg, #ffd1dc, #ffe6f0);
      text-align: center;
      overflow-x: hidden;
    }

    h1 {
      margin-top: 100px;
      font-size: 3.5em;
      color: #b3005e;
      animation: fadeIn 2s ease;
    }

    .scroll {
      margin-top: 60px;
      font-size: 1.2em;
      color: #8a0046;
      animation: bounce 2s infinite;
    }

    .envelope {
      width: 220px;
      height: 140px;
      margin: 100px auto;
      background: #ff9acb;
      position: relative;
      cursor: pointer;
      border-radius: 6px;
    }

    .envelope::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 0;
      height: 0;
      border-left: 110px solid transparent;
      border-right: 110px solid transparent;
      border-top: 70px solid #ff6fb1;
    }

    .letter {
      display: none;
      background: white;
      width: 80%;
      margin: 40px auto;
      padding: 25px;
      border-radius: 12px;
      color: #444;
      font-size: 1.1em;
      box-shadow: 0 10px 20px rgba(0,0,0,0.15);
      animation: fadeIn 1.5s ease;
    }

    .question {
      margin-top: 30px;
      font-size: 1.5em;
      color: #b3005e;
    }

    .buttons {
      margin-top: 20px;
    }

    button {
      padding: 12px 25px;
      font-size: 1em;
      border: none;
      border-radius: 20px;
      cursor: pointer;
      margin: 10px;
    }

    #yes {
      background: #ff4d94;
      color: white;
    }

    #no {
      background: #ccc;
      position: absolute;
    }

    .fireworks {
      display: none;
      font-size: 2em;
      margin-top: 40px;
      animation: fadeIn 2s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
  </style>
</head>
<body>

  <h1>t amo eram di 💗</h1>

  <div class="scroll">⬇️ desliza ⬇️</div>

  <div class="envelope" onclick="openLetter()"></div>

  <div class="letter" id="letter">
    <p>
      Desde que llegaste a mi vida,<br>
      los días normales se sienten especiales.<br>
      No necesito una fecha para quererte,<br>
      pero sí quiero pasar este San Valentín contigo.
    </p>

    <div class="question">Will you be my Valentine? 💘</div>

    <div class="buttons">
      <button id="yes" onclick="sayYes()">Yes 💖</button>
      <button id="no" onmouseover="moveNo()">No 🙄</button>
    </div>
  </div>

  <div class="fireworks" id="fireworks">
    🎆💖🎆<br>
    Sabía que dirías que sí 💕
  </div>

  <script>
    function openLetter() {
      document.getElementById("letter").style.display = "block";
      document.getElementById("letter").scrollIntoView({ behavior: "smooth" });
    }

    function sayYes() {
      document.getElementById("fireworks").style.display = "block";
      document.getElementById("fireworks").scrollIntoView({ behavior: "smooth" });
    }

    function moveNo() {
      const noBtn = document.getElementById("no");
      const x = Math.random() * (window.innerWidth - 100);
      const y = Math.random() * (window.innerHeight - 50);
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }
  </script>

</body>
</html>
